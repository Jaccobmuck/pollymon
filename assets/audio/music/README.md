# Music Assets

No third-party music tracks are imported yet.

Candidate sources are tracked in `docs/ASSET_WORKFLOW.md`. When imported, normalize filenames with the `music-` prefix, for example `music-field-loop.ogg`.
